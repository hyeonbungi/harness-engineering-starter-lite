# AGENTS.md

이 저장소는 범용 하네스 엔지니어링 스타터 자체를 설계합니다. 이 파일은
라우터로 유지하고, 세부 규칙과 이력은 연결된 문서에 둡니다.

## 요청 분류

현재 메시지의 명시적 의도를 먼저 따릅니다.

- 답변: 읽고 답하되 파일을 변경하지 않습니다.
- 진단: 필요한 범위의 읽기 전용 재현까지만 수행합니다.
- 제안: 선택지와 트레이드오프를 정리하되 구현하지 않습니다.
- 실행: 요청된 범위만 변경하고 적절한 검증까지 수행합니다.

과거 승인, 기존 계획, 우연히 발견한 드리프트만으로 현재 요청을 실행으로
승격하지 않습니다.

## 자동 구조 개선

에이전트 행동 양식, 하네스 또는 작업 루프에서 반복되거나 재현 가능한 구조적
결함을 확인하면 별도 명령 없이도 저장소 내부 최소 개선을 자동 수행합니다.
사용자의 명시적 변경 금지·중지가 우선하며, 제품 동작·외부 시스템·파괴적
작업·커밋·푸시·배포는 이 상시 권한에 포함하지 않습니다. 한 사용자 작업에서
한 번만 실행하고 기존 기능을 통합한 뒤 검증하여 종료합니다.

## 시작 순서

실행 작업을 시작하기 전에:

1. `pwd`로 저장소 루트를 확인합니다.
2. `docs/STATE.md`에서 현재 상태와 다음 행동을 읽습니다.
3. `feature_list.json` 전체 이력 대신 active 또는 가장 높은 우선순위의
   미완료 레코드만 확인합니다.
4. `docs/design-proposal.md`에서는 변경 대상과 관련된 절만 확인합니다.
5. POSIX에서는 `./init.sh`, Windows PowerShell에서는 `.\init.ps1`을
   실행합니다.

## 작업 규칙

- 한 번에 하나의 기능만 `active`로 둡니다.
- 원본 Obsidian 학습 폴더는 읽기 전용이며 수정하지 않습니다.
- 특정 스택을 범용 기본값으로 강제하지 않습니다.
- 규칙을 추가하기 전에 기존 문서·검사·코드 가까이에 둘 수 있는지 확인합니다.
- 실행 증거 없이 `passing` 또는 완료를 주장하지 않습니다.
- 검증을 통과시키기 위해 검사를 삭제하거나 약화하지 않습니다.
- 비밀을 저장소에 기록하지 않고 환경 변수와 최소 권한을 사용합니다.
- 반복되는 리뷰 의견은 가능한 경우 작고 실행 가능한 검사로 승격합니다.
- 구조적 결함은 재현·권한·종료 조건을 확인한 뒤 한 가지 최소 수정으로
  개선하고, 규칙을 계속 덧붙이지 말고 기존 내용을 통합하거나 교체합니다.
- 기준선과 종료 시 전체 init을 실행하고, 구현 중에는 관련 집중 검사를
  우선합니다.

## 문서 라우팅

- `docs/STATE.md`: 현재 목표, 검증 상태, 위험, 다음 행동
- `feature_list.json`: 범위와 상태의 기계 진실
- `docs/source-inventory.md`: 원본 자료의 완전성·무결성 원장
- `docs/source-analysis.md`: 전수 분석 결과
- `docs/design-proposal.md`: 구성 요소, 프로필, 근거 추적 및 구현 순서

## 완료 기준

변경은 다음을 모두 만족할 때만 완료입니다.

- 목표 동작 또는 문서 결과가 요청 범위와 일치합니다.
- 필요한 검증을 실제 실행했습니다.
- 결과와 한계를 `feature_list.json` 또는 `docs/STATE.md`에 기록했습니다.
- 다음 세션이 표준 시작 경로로 재개할 수 있습니다.
- 변경한 구성 요소의 근거가 추적 가능합니다.

## 종료 순서

1. 관련 기능의 상태와 증거를 갱신합니다.
2. `docs/STATE.md`를 현재 사실만 남도록 갱신합니다.
3. 현재 운영체제의 init 어댑터를 재실행합니다.
4. 임시 파일과 미설명 변경을 남기지 않습니다.
5. 커밋·푸시·외부 동작은 사용자의 명시적 요청이 있을 때만 수행합니다.
